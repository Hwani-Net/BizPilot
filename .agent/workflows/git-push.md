---
description: Git add, commit and push all changes
---
// turbo-all
1. Stage all changes
```
git add -A
```
2. Show status
```
git status
```
3. Commit with message (replace MESSAGE with appropriate commit message)
```
git commit -m "MESSAGE"
```
4. Push to remote
```
git push
```
